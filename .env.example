# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=cambiar_en_produccion
PG_PORT=5440          # puerto del host (solo para dev; en prod no es necesario exponerlo)

# ── Backend ───────────────────────────────────────────────────────────────────
JWT_SECRET=cambiar_por_un_secreto_largo_y_aleatorio
ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com
ENVIRONMENT=production

# ── MySQL origen (migración one-shot) ────────────────────────────────────────
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DB_LOGISTICA=logistica
MYSQL_DB_BASES_WEB=bases_web
